[project]
name = "kalshi-rss"
version = "0.1.0"
description = "Kalshi RSS feed"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["requests"]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk", "pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
